/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["swiperjs.com", "flic.kr", "live.staticflickr.com"],
  },
};

module.exports = nextConfig;
